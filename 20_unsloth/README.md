

# Install 

```bash
$ uv pip install -r requirements.txt --upgrade --index-strategy unsafe-best-match
``

$ uv pip install --upgrade --no-deps transformers==4.56.2 trl==0.22.2 tokenizers
$ uv pip install --upgrade --no-deps transformers==4.57.6 trl==0.22.2 tokenizers
```